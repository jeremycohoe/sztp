<config xmlns="urn:ietf:params:xml:ns:netconf:base:1.0">
  <!--
    SZTP first-stage configuration — INTENTIONALLY MINIMAL.

    All day-0 configuration is performed by the pre-configuration script
    (config/first-pre-configuration-script.sh, which is ztp-simple.py).
    The sZTP onboarding-information response still requires a configuration
    element, so this file exists only as a benign no-op merge.

    Do not add device configuration here — put it in the pre-script.
  -->
  <native xmlns="http://cisco.com/ns/yang/Cisco-IOS-XE-native">
    <service>
      <password-encryption/>
    </service>
  </native>
</config>
